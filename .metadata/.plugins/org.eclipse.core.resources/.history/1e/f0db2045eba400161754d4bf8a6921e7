class Semaphore {
	private int permits;

	public Semaphore(int initialPermits) {
		permits = initialPermits;
	}

	public void acquire() {
		permits = permits - 1;
		// compléter
                // if ...
	}

	public void release() {
		permits = permits + 1;
		// compléter
		// if ...
	}
}
